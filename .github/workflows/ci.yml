name: CI

on:
  push:
    branches: ["**"]

jobs:
  lint-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install uv
        run: pip install uv

      - name: Install dependencies
        run: uv sync

      - name: Ruff format
        run: uv run python3 -m ruff format . --check

      - name: Ruff linter
        run: uv run python3 -m ruff check . --fix

      - name: Mypy
        run: uv run python3 -m mypy .

      - name: Unit tests
        run: uv run python3 -m pytest ./core/tests/unit

      - name: Integration tests
        run: uv run python3 -m pytest ./core/tests/integration

      - name: BDD tests
        run: uv run python3 -m pytest ./core/tests/behaviour